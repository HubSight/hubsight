/**
 * The REST API base resolution is shared with `@hubsight/realtime` so the event
 * bus and the HTTP client always agree on where the gateway lives.
 */
export { resolveApiBase } from '@hubsight/realtime';
